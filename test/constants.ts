import * as path from 'path';

export const TEST_LAYOUT_ROOT = path.join(__dirname, 'pythonEnvironments', 'common', 'envlayouts');

export const TEST_DATA_ROOT = path.join(__dirname, 'pythonEnvironments', 'common', 'testdata');
