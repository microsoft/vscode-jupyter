# Dummy pipeline for Component Gov.

pr:
    autoCancel: true
    branches:
      include:
        - 'main'
        - 'release*'

# Not the CI build for merges to main and release.
trigger: none

pool:
    vmImage: 'ubuntu-latest'

steps:
    - task: ComponentGovernanceComponentDetection@0
      inputs:
        scanType: 'Register'
        verbosity: 'Verbose'
        alertWarningLevel: 'High'